package baristat.Weather;

/**
 * This is the WeatherData enum class that lists types of weather data.
 *
 * @author adrianturcu
 *
 */
public enum WeatherData {
  AVG_TEMP, MAX_TEMP, MIN_TEMP, INCHES_PRECIP,
}
